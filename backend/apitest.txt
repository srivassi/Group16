messages=[{"role": "system", "content": "This GPT, LSM Bot , is specialized in performing Linguistic Style Matching ."}]

with open('testCode.py', mode="r", encoding="utf-8") as theCode:
  fixIt = theCode.read()

print(fixIt)

response = ""

while True:

  userInput = input("André: ")
  
  if userInput.lower() in ["check this code", "use my code"]:
    messages.append({"role": "user", "content": "please fix this code: " + fixIt}),
  else:
    messages.append({"role": "user", "content": userInput}),
      
  completion = client.chat.completions.create(
    model="gpt-3.5-turbo-0125",
    messages=messages,
    temperature=0.5,
    max_tokens=512,
    pl_tags=["getting-started"]
  )
  response = completion.choices[0].message
  messages.append(response)
  print(response)